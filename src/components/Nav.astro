---
import { CONFIG } from '../data/config';

const navItems = [
  { id: 'servicios', label: 'Servicios' },
  { id: 'equipos', label: 'Equipos' },
  { id: 'nosotros', label: 'Nosotros' },
  { id: 'contacto', label: 'Contacto' }
];
---

<nav class="fixed top-0 left-0 w-full z-[100] h-[68px] bg-white/95 backdrop-blur-md border-b border-border transition-all duration-300">
  <div class="container-custom h-full px-[5%] flex items-center justify-between">
    
    <!-- Logo -->
    <!-- Logo -->
    <!-- Logo -->
    <!-- Logo -->
    <!-- Logo -->
    <a href="/" class="flex items-center group focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 rounded-lg transition-shadow" aria-label={`Inicio de ${CONFIG.empresa.nombre}`}>
    <!-- Logo -->
    <!-- IMPORTANT: BRAND LOGO - DO NOT REMOVE -->
    <!-- IMPORTANT: BRAND LOGO - DO NOT REMOVE
         This is the primary branding and navigation element to the home page. -->
    <a href="/" class="flex items-center group focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 rounded-lg transition-shadow" aria-label={`Inicio de ${CONFIG.empresa.nombre}`}>
      <img
        src="/logo-name.svg"
        alt={`${CONFIG.empresa.nombre} ${CONFIG.empresa.subtitulo}`}
        width="228"
        height="56"
        decoding="async"
        fetchpriority="high"
        class="h-[56px] w-auto drop-shadow-sm group-hover:scale-105 group-focus-visible:scale-105 transition-transform duration-300"
      />
    </a>

    <!-- Desktop Navigation -->
    <!-- Desktop Menu -->
    <ul class="hidden md:flex items-center gap-8">
      {navItems.map(item => (
        <li>
          <a href={`#${item.id}`} class="nav-link font-body font-medium text-[14px] text-text-soft hover:text-navy transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm px-1 py-0.5 capitalize">
          <a href={`#${item.id}`} class="nav-link font-body font-medium text-[14px] text-text-soft hover:text-navy transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm px-1 py-0.5">
            {item.label}
          </a>
        </li>
      ))}
    </ul>

    <div class="flex items-center gap-4">
      <!-- Desktop WhatsApp Link -->
      <a href={`https://wa.me/${CONFIG.contacto.whatsapp}`} target="_blank" rel="noopener noreferrer" class="hidden md:inline-flex bg-navy text-white font-heading font-bold text-[14px] py-[10px] px-[20px] rounded-[8px] hover:bg-navy-dark focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 transition-colors shadow-sm hover:shadow-md">
        Consultar por WhatsApp
      </a>

      <a href={`https://wa.me/${CONFIG.contacto.whatsapp}`} target="_blank" rel="noopener noreferrer" class="hidden md:inline-flex bg-navy text-white font-heading font-bold text-[14px] py-[10px] px-[20px] rounded-[8px] hover:bg-navy-dark focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 transition-colors shadow-sm hover:shadow-md">
        Consultar por WhatsApp
      </a>

      <button id="menu-toggle" class="md:hidden flex flex-col justify-center items-center w-8 h-8 space-y-1.5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 rounded-sm transition-shadow z-[101]" aria-label="Menú" aria-expanded="false" aria-controls="mobile-menu">
        <span class="block w-6 h-0.5 bg-navy transition-transform duration-300 origin-center"></span>
        <span class="block w-6 h-0.5 bg-navy transition-opacity duration-300"></span>
        <span class="block w-6 h-0.5 bg-navy transition-transform duration-300 origin-center"></span>
      <!-- Hamburger Button -->
      <button
        id="menu-toggle"
        class="md:hidden flex flex-col justify-center items-center w-8 h-8 space-y-1.5 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 rounded-sm transition-shadow z-[101]"
        aria-label="Menú"
        aria-expanded="false"
        aria-controls="mobile-menu"
      >
        <span class="block w-6 h-0.5 bg-navy transition-all duration-300 origin-center"></span>
        <span class="block w-6 h-0.5 bg-navy transition-all duration-300"></span>
        <span class="block w-6 h-0.5 bg-navy transition-all duration-300 origin-center"></span>
      </button>
    </div>
  </div>

  <!-- Mobile Menu -->
  <div id="mobile-menu" class="absolute top-[68px] left-0 w-full bg-white border-b border-border flex flex-col items-center py-6 gap-6 shadow-card-hover opacity-0 pointer-events-none translate-y-[-10px] transition-all duration-300 ease-in-out md:hidden z-[99]" aria-hidden="true">
    {navItems.map(item => (
      <a href={`#${item.id}`} class="nav-link mobile-link font-body font-medium text-[16px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors capitalize">
  <div id="mobile-menu" class="absolute top-[68px] left-0 w-full bg-white border-b border-border flex flex-col items-center py-8 gap-6 shadow-card-hover opacity-0 pointer-events-none translate-y-[-10px] transition-all duration-300 ease-in-out md:hidden z-[99]" aria-hidden="true">
    <a href="#servicios" class="nav-link mobile-link font-body font-medium text-[18px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors">Servicios</a>
    <a href="#equipos" class="nav-link mobile-link font-body font-medium text-[18px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors">Equipos</a>
    <a href="#nosotros" class="nav-link mobile-link font-body font-medium text-[18px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors">Nosotros</a>
    <a href="#contacto" class="nav-link mobile-link font-body font-medium text-[18px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors">Contacto</a>
    <a href={`https://wa.me/${CONFIG.contacto.whatsapp}`} target="_blank" rel="noopener noreferrer" class="mt-2 bg-navy text-white font-heading font-bold text-[15px] py-[12px] px-[24px] rounded-[8px] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 transition-colors">
  <div id="mobile-menu" class="absolute top-[68px] left-0 w-full bg-white border-b border-border flex flex-col items-center py-6 gap-6 shadow-card-hover opacity-0 pointer-events-none translate-y-[-10px] transition-all duration-300 ease-in-out md:hidden z-[99]" aria-hidden="true">
    {navItems.map(item => (
      <a href={`#${item.id}`} class="nav-link mobile-link font-body font-medium text-[16px] text-text-soft hover:text-navy focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-2 rounded-sm transition-colors">
        {item.label}
      </a>
    ))}
    <a href={`https://wa.me/${CONFIG.contacto.whatsapp}`} id="whatsapp-mobile-link" target="_blank" rel="noopener noreferrer" class="mt-2 bg-navy text-white font-heading font-bold text-[14px] py-[12px] px-[24px] rounded-[8px] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue focus-visible:ring-offset-4 transition-colors">
      Consultar por WhatsApp
    </a>
  </div>
</nav>

<style>
  .nav-link-active { @apply text-blue !important; }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const toggle = document.getElementById('menu-toggle');
    const menu = document.getElementById('mobile-menu');
    const navLinks = document.querySelectorAll('.nav-link');
    const mobileLinks = document.querySelectorAll('.mobile-link');
    const whatsappMobileLink = document.getElementById('whatsapp-mobile-link');
    const spans = toggle?.querySelectorAll('span');
    let isOpen = false;

    let isOpen = false;


    let isOpen = false;

    let isOpen = false;

    const toggleMenu = () => {
      isOpen = !isOpen;

    const toggleMenu = () => {
      isOpen = !isOpen;
      toggle?.setAttribute('aria-expanded', isOpen.toString());
      menu?.setAttribute('aria-hidden', (!isOpen).toString());

      if (menu) {
        menu.classList.toggle('opacity-0', !isOpen);
        menu.classList.toggle('pointer-events-none', !isOpen);
        menu.classList.toggle('translate-y-[-10px]', !isOpen);
        menu.classList.toggle('opacity-100', isOpen);
        menu.classList.toggle('pointer-events-auto', isOpen);
        menu.classList.toggle('translate-y-0', isOpen);
      }
      if (isOpen) {
        menu?.classList.remove('opacity-0', 'pointer-events-none', 'translate-y-[-10px]');
        menu?.classList.add('opacity-100', 'pointer-events-auto', 'translate-y-0');
      } else {
        menu?.classList.add('opacity-0', 'pointer-events-none', 'translate-y-[-10px]');
        menu?.classList.remove('opacity-100', 'pointer-events-auto', 'translate-y-0');
      }
      menu?.classList.toggle('opacity-0', !isOpen);
      menu?.classList.toggle('pointer-events-none', !isOpen);
      menu?.classList.toggle('translate-y-[-10px]', !isOpen);
      menu?.classList.toggle('opacity-100', isOpen);
      menu?.classList.toggle('pointer-events-auto', isOpen);
      menu?.classList.toggle('translate-y-0', isOpen);

      if (spans?.length === 3) {
        spans[0].classList.toggle('rotate-45', isOpen);
        spans[0].classList.toggle('translate-y-[8px]', isOpen);
        spans[1].classList.toggle('opacity-0', isOpen);
        spans[2].classList.toggle('-rotate-45', isOpen);
        spans[2].classList.toggle('-translate-y-[8px]', isOpen);
      }
    };

    toggle?.addEventListener('click', toggleMenu);
    if (toggle) toggle.addEventListener('click', toggleMenu);

    mobileLinks.forEach(link => {
      link.addEventListener('click', () => {
        if (isOpen) toggleMenu();
      });
    });

    // Close on Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isOpen) {
        toggleMenu();
        (toggle as HTMLElement | null)?.focus();
      }
    });

    // Mobile menu focus trap
    document.addEventListener('keydown', (e) => {
      if (!isOpen) return;

    // Keyboard handling
    // Keyboard handling: Escape key & Focus trap
    // Keyboard handling: Focus trapping & Escape key
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape' && isOpen) {
        toggleMenu();
        (toggle as HTMLElement)?.focus();
        return;
      }

      if (isOpen && e.key === 'Tab') {
        const focusable = [toggle, ...Array.from(mobileLinks), whatsappMobileLink].filter(Boolean);
        const first = focusable[0] as HTMLElement;
        const last = focusable[focusable.length - 1] as HTMLElement;

        if (e.shiftKey && document.activeElement === first) {
          e.preventDefault();
          last.focus();
        } else if (!e.shiftKey && document.activeElement === last) {
          e.preventDefault();
          first.focus();
        if (e.shiftKey) { // Shift + Tab
          if (document.activeElement === toggle) {
            e.preventDefault();
            (whatsappMobileLink as HTMLElement)?.focus();
          }
        } else { // Tab
          if (document.activeElement === whatsappMobileLink) {
            e.preventDefault();
            toggle?.focus();
          }
        }
      }
    });

    // ScrollSpy implementation
      if (e.key === 'Tab') {
        const focusableElements = [toggle, ...Array.from(mobileLinks), whatsappMobileLink].filter(Boolean) as HTMLElement[];
        const firstElement = focusableElements[0];
        const lastElement = focusableElements[focusableElements.length - 1];

        if (e.shiftKey) { // Shift + Tab
          if (document.activeElement === firstElement) {
            e.preventDefault();
            lastElement?.focus();
          }
        } else { // Tab
          if (document.activeElement === lastElement) {
            e.preventDefault();
            firstElement?.focus();
          }
        }
      }
    });

    // ScrollSpy Implementation
    const observerOptions = {
      root: null,
      rootMargin: '-20% 0px -70% 0px',
      threshold: 0
    };

    const observer = new IntersectionObserver((entries) => {
    const observerCallback = (entries: IntersectionObserverEntry[]) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          const id = entry.target.getAttribute('id');
          navLinks.forEach(link => {
            const href = link.getAttribute('href');
            if (href === `#${id}`) {
              link.classList.add('nav-link-active');
            const isActive = link.getAttribute('href') === `#${id}`;
            link.classList.toggle('nav-link-active', isActive);
            if (isActive) {
            const isActive = link.getAttribute('href') === '#' + id;
            link.classList.toggle('nav-link-active', isActive);
            if (isActive) {
              link.setAttribute('aria-current', 'location');
            } else {
              link.classList.remove('nav-link-active');
              link.removeAttribute('aria-current');
            }
          });
        }
      });
    }, observerOptions);

    document.querySelectorAll('section[id]').forEach(section => observer.observe(section));

    // Reset ScrollSpy when clicking logo
    document.querySelector('a[aria-label^="Inicio"]')?.addEventListener('click', () => {
    }, observerOptions);

    };

    const observer = new IntersectionObserver(observerCallback, observerOptions);
    ['servicios', 'equipos', 'nosotros', 'contacto'].forEach(id => {
      const section = document.getElementById(id);
      if (section) observer.observe(section);
    });

    // Reset ScrollSpy when clicking logo
      const el = document.getElementById(id);
      if (el) observer.observe(el);
    });

    // Reset ScrollSpy when clicking logo

    // Reset ScrollSpy when clicking logo
    document.querySelector('a[aria-label^="Inicio de"]')?.addEventListener('click', () => {
    const sections = document.querySelectorAll('section[id]');
    sections.forEach(section => observer.observe(section));

    // Reset ScrollSpy when clicking logo
    document.querySelector('a[aria-label^="Inicio"]')?.addEventListener('click', () => {
    ['servicios', 'equipos', 'nosotros', 'contacto'].forEach(id => {
      const el = document.getElementById(id);
      if (el) observer.observe(el);
    });

    // Reset ScrollSpy when clicking logo/home
    document.querySelector('a[aria-label^="Inicio de"]')?.addEventListener('click', () => {
      navLinks.forEach(link => {
        link.classList.remove('nav-link-active');
        link.removeAttribute('aria-current');
      });
    });

    // Keyboard handling: Focus trapping & Escape key
    document.addEventListener('keydown', (e) => {
      if (!isOpen) return;

      if (e.key === 'Escape') {
        toggleMenu();
        (toggle as HTMLElement)?.focus();
        if (toggle && 'focus' in toggle) (toggle as HTMLElement).focus();
        return;
      }

      if (e.key === 'Tab') {
        const focusableElements = [toggle, ...Array.from(mobileLinks), whatsappMobileLink].filter(Boolean) as HTMLElement[];
        const firstElement = focusableElements[0];
        const lastElement = focusableElements[focusableElements.length - 1];

        if (e.shiftKey) { // Shift + Tab
          if (document.activeElement === firstElement) {
            e.preventDefault();
            lastElement?.focus();
            if (lastElement) lastElement.focus();
          }
        } else { // Tab
          if (document.activeElement === lastElement) {
            e.preventDefault();
            firstElement?.focus();
          }
        }
      }
    });
            if (firstElement) firstElement.focus();
          }
        }
      }
    });
    }, observerOptions);

    sections.forEach(section => {
      observer.observe(section);
    });

    // Reset ScrollSpy when clicking logo
    document.querySelector('a[aria-label="Inicio de TechHealth"]')?.addEventListener('click', () => {
      navLinks.forEach(link => {
        link.classList.remove('nav-link-active');
        link.removeAttribute('aria-current');
      });
    }, { rootMargin: '-20% 0px -70% 0px' });

    const observer = new IntersectionObserver(observerCallback, observerOptions);

    // Observe all sections that have a corresponding nav-link
    const sections = ['servicios', 'equipos', 'nosotros', 'contacto'];
    sections.forEach(id => {
      const section = document.getElementById(id);
      if (section) observer.observe(section);
    });
    document.querySelectorAll('section[id]').forEach(section => observer.observe(section));

    // Reset ScrollSpy when clicking logo
    document.querySelector('a[aria-label^="Inicio de"]')?.addEventListener('click', () => {
      navLinks.forEach(link => {
        link.classList.remove('nav-link-active');
        link.removeAttribute('aria-current');
      });
    });
  });
</script>

<style>
  .nav-link-active {
    @apply text-navy !important;
    position: relative;
  }

  .nav-link-active::after {
    content: '';
    position: absolute;
    bottom: -4px;
    left: 0;
    width: 100%;
    height: 2px;
    @apply bg-blue rounded-full;
  }

  @media (max-width: 767px) {
    .nav-link-active::after {
      display: none;
    }
  }
</style>
